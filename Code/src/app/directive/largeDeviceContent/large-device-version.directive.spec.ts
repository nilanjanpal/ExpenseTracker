import { LargeDeviceVersionDirective } from './large-device-version.directive';

describe('LargeDeviceVersionDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeDeviceVersionDirective();
    expect(directive).toBeTruthy();
  });
});

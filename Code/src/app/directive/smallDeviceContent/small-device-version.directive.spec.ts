import { SmallDeviceVersionDirective } from './small-device-version.directive';

describe('SmallDeviceVersionDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallDeviceVersionDirective();
    expect(directive).toBeTruthy();
  });
});
